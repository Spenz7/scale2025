specVersion: "3.1"
metadata:
  name: project1
type: application
server:
  customMiddleware:
    - name: proxy
      afterMiddleware: compression
      configuration:
        backend:
          - path: /engagement
            url: http://localhost:4005/engagement
            changeOrigin: true